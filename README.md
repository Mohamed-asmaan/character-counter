# Character Counter

A simple web app that counts characters in real time as you type, with a 200-character limit and visual feedback when the limit is reached.

## Features

- Live character count (current / max)
- Remaining characters display
- Warning message when limit is reached
- Red border on textarea at limit
- Traditional HTML, CSS, and JavaScript — beginner-friendly, no frameworks

## How to Run

1. Open `index.html` in a web browser
2. Or use a local server (e.g. `npx serve .`)

## Project Structure

```
character-counter/
├── index.html   # Main page
├── style.css    # Styles
├── script.js    # Character counting logic
└── README.md
```